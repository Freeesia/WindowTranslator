<Window x:Class="WindowTranslator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowTranslator"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:MainViewModel}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!--<local:HwndHostPresenter WindowHandle="{Binding WindowHandle}" />-->
        <local:WindowCaptureCompositionhost TargetWindow="{Binding WindowHandle}"/>
        <TextBlock Text="{Binding OcrText}"/>
    </Grid>
</Window>

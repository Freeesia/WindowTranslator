<UserControl
    x:Class="WindowTranslator.Modules.PluginStore.PluginStoreView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WindowTranslator.Modules.PluginStore"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:properties="clr-namespace:WindowTranslator.Properties"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DataContext="{d:DesignInstance {x:Type local:PluginStoreViewModel}}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="b2vConv" />
        <Style x:Key="InstallButtonStyle" BasedOn="{StaticResource {x:Type ui:Button}}" TargetType="ui:Button">
            <Setter Property="Margin" Value="4,2" />
            <Setter Property="MinWidth" Value="90" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  エラーメッセージ  -->
        <ui:InfoBar
            Grid.Row="0"
            Margin="4"
            IsClosable="False"
            IsOpen="{Binding ErrorMessage, Converter={x:Static local:NotNullToBoolConverter.Default}}"
            Message="{Binding ErrorMessage}"
            Severity="Error" />

        <!--  ローディング中  -->
        <ProgressBar
            Grid.Row="0"
            Height="4"
            IsIndeterminate="True"
            Visibility="{Binding IsLoading, Converter={StaticResource b2vConv}}" />

        <!--  パッケージ一覧と詳細  -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="300" MinWidth="200" />
            </Grid.ColumnDefinitions>

            <!--  パッケージ一覧  -->
            <ui:ListView
                Grid.Column="0"
                Margin="4"
                ItemsSource="{Binding Packages}"
                SelectedItem="{Binding SelectedPackage}"
                ScrollViewer.HorizontalScrollBarVisibility="Hidden">
                <ui:ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource {x:Type ui:ListViewItem}}" TargetType="ui:ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ui:ListView.ItemContainerStyle>
                <ui:ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:PluginPackageViewModel}">
                        <Grid Margin="4,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!--  タイトルと状態  -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                <ui:TextBlock
                                    VerticalAlignment="Center"
                                    FontWeight="SemiBold"
                                    Text="{Binding Title}"
                                    TextTrimming="CharacterEllipsis" />
                                <ui:Badge
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Center"
                                    Appearance="Success"
                                    Content="{x:Static properties:Resources.Installed}"
                                    Visibility="{Binding IsInstalled, Converter={StaticResource b2vConv}}" />
                                <ui:Badge
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Center"
                                    Appearance="Caution"
                                    Content="{x:Static properties:Resources.UpdateAvailable}"
                                    Visibility="{Binding IsUpdateAvailable, Converter={StaticResource b2vConv}}" />
                            </StackPanel>

                            <!--  バージョン情報  -->
                            <ui:TextBlock
                                Grid.Row="1"
                                Grid.Column="0"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="{Binding StatusText}"
                                TextTrimming="CharacterEllipsis" />

                            <!--  インストールボタン  -->
                            <StackPanel
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <!--  インストールプログレス  -->
                                <ProgressBar
                                    Width="60"
                                    Height="4"
                                    Margin="4,0"
                                    VerticalAlignment="Center"
                                    Value="{Binding InstallProgress}"
                                    Visibility="{Binding IsInstalling, Converter={StaticResource b2vConv}}" />

                                <!--  インストールボタン（未インストール）  -->
                                <ui:Button
                                    Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Content="{x:Static properties:Resources.Install}"
                                    Icon="{ui:SymbolIcon ArrowDownload24}"
                                    IsEnabled="{Binding IsInstalling, Converter={x:Static local:InverseBoolConverter.Default}}"
                                    Style="{StaticResource InstallButtonStyle}"
                                    Visibility="{Binding IsInstalled, Converter={x:Static local:InverseBoolConverter.Default}, ConverterParameter=Visibility}" />

                                <!--  更新ボタン（更新あり）  -->
                                <ui:Button
                                    Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Content="{x:Static properties:Resources.Update}"
                                    Icon="{ui:SymbolIcon ArrowSync24}"
                                    IsEnabled="{Binding IsInstalling, Converter={x:Static local:InverseBoolConverter.Default}}"
                                    Style="{StaticResource InstallButtonStyle}"
                                    Visibility="{Binding IsUpdateAvailable, Converter={StaticResource b2vConv}}" />

                                <!--  アンインストールボタン（インストール済み）  -->
                                <ui:Button
                                    Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Content="{x:Static properties:Resources.Uninstall}"
                                    Icon="{ui:SymbolIcon Delete24}"
                                    IsEnabled="{Binding IsInstalling, Converter={x:Static local:InverseBoolConverter.Default}}"
                                    Style="{StaticResource InstallButtonStyle}"
                                    Visibility="{Binding IsInstalled, Converter={StaticResource b2vConv}}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>

            <GridSplitter
                Grid.Column="1"
                Width="4"
                HorizontalAlignment="Center"
                VerticalAlignment="Stretch"
                Background="{DynamicResource SeparatorBorderBrush}"
                ResizeBehavior="PreviousAndNext" />

            <!--  パッケージ詳細  -->
            <ScrollViewer
                Grid.Column="2"
                Margin="8"
                VerticalScrollBarVisibility="Auto">
                <StackPanel DataContext="{Binding SelectedPackage}">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.SelectedPackage, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <ui:TextBlock
                        Margin="0,0,0,4"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="{Binding Title}"
                        TextWrapping="Wrap" />

                    <ui:TextBlock
                        Margin="0,0,0,8"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{Binding Authors}"
                        TextWrapping="Wrap" />

                    <Separator Margin="0,0,0,8" />

                    <ui:TextBlock
                        Margin="0,0,0,8"
                        Text="{Binding Description}"
                        TextWrapping="Wrap" />

                    <!--  バージョン情報  -->
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Label
                            Grid.Row="0"
                            Grid.Column="0"
                            Content="{x:Static properties:Resources.LatestVersion}"
                            Padding="0,4,8,4" />
                        <ui:TextBlock
                            Grid.Row="0"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Text="{Binding LatestVersion}" />

                        <Label
                            Grid.Row="1"
                            Grid.Column="0"
                            Content="{x:Static properties:Resources.InstalledVersionLabel}"
                            Padding="0,4,8,4"
                            Visibility="{Binding IsInstalled, Converter={StaticResource b2vConv}}" />
                        <ui:TextBlock
                            Grid.Row="1"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Text="{Binding InstalledVersion}"
                            Visibility="{Binding IsInstalled, Converter={StaticResource b2vConv}}" />
                    </Grid>

                    <Separator Margin="0,4,0,8" />

                    <!--  リンク  -->
                    <ui:HyperlinkButton
                        Content="{x:Static properties:Resources.ProjectUrl}"
                        Icon="{ui:SymbolIcon Globe24}"
                        NavigateUri="{Binding ProjectUrl}"
                        Visibility="{Binding ProjectUrl, Converter={x:Static local:NotNullToVisibilityConverter.Default}}" />

                    <ui:HyperlinkButton
                        Content="{x:Static properties:Resources.LicenseUrl}"
                        Icon="{ui:SymbolIcon Document24}"
                        NavigateUri="{Binding LicenseUrl}"
                        Visibility="{Binding LicenseUrl, Converter={x:Static local:NotNullToVisibilityConverter.Default}}" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>

<UserControl
    x:Class="WindowTranslator.Modules.PluginStore.PluginStoreView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:WindowTranslator.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="clr-namespace:WindowTranslator.Data"
    xmlns:local="clr-namespace:WindowTranslator.Modules.PluginStore"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="https://github.com/whistyun/MdXaml"
    xmlns:mdp="clr-namespace:MdXaml.Plugins;assembly=MdXaml.Plugins"
    xmlns:properties="clr-namespace:WindowTranslator.Properties"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DataContext="{d:DesignInstance {x:Type local:PluginStoreViewModel}}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="b2vConv" />
        <Style
            x:Key="InstallButtonStyle"
            BasedOn="{StaticResource {x:Type ui:Button}}"
            TargetType="ui:Button">
            <Setter Property="Margin" Value="4,2" />
            <Setter Property="MinWidth" Value="90" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  エラーメッセージ  -->
        <ui:InfoBar
            Grid.Row="0"
            Margin="4"
            IsClosable="False"
            IsOpen="{Binding HasError}"
            Message="{Binding ErrorMessage}"
            Severity="Error" />

        <!--  パッケージ一覧と詳細  -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="200" />
            </Grid.ColumnDefinitions>

            <!--  パッケージ一覧  -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ui:ListView
                    Grid.Row="0"
                    Margin="4"
                    ItemsSource="{Binding Packages}"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    SelectedItem="{Binding SelectedPackage}"
                    VirtualizingPanel.ScrollUnit="Pixel">
                    <ui:ListView.ItemContainerStyle>
                        <Style BasedOn="{StaticResource {x:Type ui:ListViewItem}}" TargetType="ui:ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ui:ListView.ItemContainerStyle>
                    <ui:ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:PluginPackageViewModel}">
                            <Grid Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Border
                                    Grid.RowSpan="2"
                                    Width="40"
                                    Height="40"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Top"
                                    ClipToBounds="True"
                                    CornerRadius="4">
                                    <Grid>
                                        <Image
                                            RenderOptions.BitmapScalingMode="HighQuality"
                                            Source="/WindowTranslator;component/Assets/plugin-icon.png"
                                            Stretch="UniformToFill" />
                                        <Image
                                            RenderOptions.BitmapScalingMode="HighQuality"
                                            Source="{Binding IconUrl}"
                                            Stretch="UniformToFill" />
                                        <ui:SymbolIcon
                                            Width="24"
                                            Height="24"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            Filled="True"
                                            Foreground="{ui:ThemeResource SystemFillColorSuccessBrush}"
                                            Symbol="CheckmarkCircle24"
                                            ToolTip="{x:Static properties:Resources.Installed}">
                                            <ui:SymbolIcon.Style>
                                                <Style TargetType="{x:Type ui:SymbolIcon}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding IsInstalled}" Value="True" />
                                                                <Condition Binding="{Binding IsUpdateAvailable}" Value="False" />
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:SymbolIcon.Style>
                                        </ui:SymbolIcon>
                                        <ui:SymbolIcon
                                            Width="24"
                                            Height="24"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            Filled="True"
                                            Foreground="{ui:ThemeResource SystemFillColorCautionBrush}"
                                            Symbol="Alert24"
                                            ToolTip="{x:Static properties:Resources.UpdateAvailable}"
                                            Visibility="{Binding IsUpdateAvailable, Converter={StaticResource b2vConv}}" />
                                    </Grid>
                                </Border>

                                <!--  タイトルと状態  -->
                                <DockPanel Grid.Column="1" LastChildFill="True">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <ui:TextBlock
                                            VerticalAlignment="Center"
                                            FontWeight="SemiBold"
                                            Text="{Binding Title}"
                                            TextTrimming="CharacterEllipsis"
                                            TextWrapping="NoWrap" />
                                        <ui:SymbolIcon
                                            Grid.Column="1"
                                            Margin="4,0,0,0"
                                            VerticalAlignment="Center"
                                            Filled="True"
                                            Foreground="{ui:ThemeResource AccentTextFillColorSecondaryBrush}"
                                            Symbol="CheckmarkCircle24"
                                            ToolTip="{x:Static properties:Resources.PluginOfficial}"
                                            Visibility="{Binding IsOfficial, Converter={StaticResource b2vConv}}" />
                                    </Grid>
                                </DockPanel>
                                <ui:TextBlock
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Top"
                                    FontSize="12"
                                    Text="{Binding Description}"
                                    TextWrapping="Wrap" />
                            </Grid>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                </ui:ListView>

                <Border
                    Grid.Row="1"
                    Margin="4"
                    Padding="8"
                    BorderBrush="{DynamicResource SeparatorBorderBrush}"
                    BorderThickness="1,1,0,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HideDisclaimer}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <ui:TextBlock
                            FontSize="12"
                            Text="{x:Static properties:Resources.PluginStoreDisclaimer}"
                            TextWrapping="Wrap" />
                        <CheckBox
                            Margin="0,8,0,0"
                            Content="{x:Static properties:Resources.ReviewNeverShowAgain}"
                            IsChecked="{Binding HideDisclaimer, Mode=TwoWay}" />
                    </StackPanel>
                </Border>
            </Grid>

            <GridSplitter
                Grid.Column="1"
                Width="4"
                HorizontalAlignment="Center"
                VerticalAlignment="Stretch"
                Background="{DynamicResource SeparatorBorderBrush}"
                ResizeBehavior="PreviousAndNext" />

            <!--  パッケージ詳細  -->
            <Grid
                Grid.Column="2"
                Margin="8"
                DataContext="{Binding SelectedPackage}">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DataContext.SelectedPackage, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ScrollViewer
                    Grid.Row="0"
                    MaxHeight="300"
                    VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border
                                Width="48"
                                Height="48"
                                Margin="0,0,8,0"
                                VerticalAlignment="Top"
                                ClipToBounds="True"
                                CornerRadius="4">
                                <Grid>
                                    <Image
                                        RenderOptions.BitmapScalingMode="HighQuality"
                                        Source="/WindowTranslator;component/Assets/plugin-icon.png"
                                        Stretch="UniformToFill" />
                                    <Image
                                        RenderOptions.BitmapScalingMode="HighQuality"
                                        Source="{Binding IconUrl}"
                                        Stretch="UniformToFill" />
                                </Grid>
                            </Border>

                            <StackPanel Grid.Column="1">
                                <DockPanel LastChildFill="True">
                                    <ui:SymbolIcon
                                        Margin="4,0,0,0"
                                        VerticalAlignment="Center"
                                        DockPanel.Dock="Right"
                                        Filled="True"
                                        Foreground="{ui:ThemeResource AccentTextFillColorSecondaryBrush}"
                                        Symbol="CheckmarkCircle24"
                                        ToolTip="{x:Static properties:Resources.PluginOfficial}"
                                        Visibility="{Binding IsOfficial, Converter={StaticResource b2vConv}}" />
                                    <ui:TextBlock
                                        FontSize="16"
                                        FontWeight="SemiBold"
                                        Text="{Binding Title}"
                                        TextWrapping="Wrap" />
                                </DockPanel>
                                <ui:TextBlock
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Authors}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>

                        <Separator Margin="0,0,0,8" />

                        <ui:TextBlock
                            Margin="0,0,0,8"
                            Text="{Binding Description}"
                            TextWrapping="Wrap" />

                        <DockPanel HorizontalAlignment="Stretch">
                            <CheckBox
                                Margin="0,2,0,0"
                                Content="{x:Static properties:Resources.Prerelease}"
                                IsChecked="{Binding UsePrerelease, Mode=TwoWay}"
                                IsEnabled="{Binding IsInstalling, Converter={x:Static data:InverseBoolConverter.Default}}"
                                ToolTip="{Binding PrereleaseVersion}"
                                Visibility="{Binding HasPrereleaseVersion, Converter={StaticResource b2vConv}}" />
                            <!--  インストールプログレス  -->
                            <ui:ProgressRing
                                Height="20"
                                Margin="4,0"
                                VerticalAlignment="Center"
                                DockPanel.Dock="Right"
                                Progress="{Binding InstallProgress}"
                                Visibility="{Binding IsInstalling, Converter={StaticResource b2vConv}}" />

                        </DockPanel>
                        <!--  バージョン情報  -->
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Label
                                Grid.Row="0"
                                Grid.Column="0"
                                Content="{x:Static properties:Resources.LatestVersion}" />
                            <ui:TextBlock
                                Grid.Row="0"
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Text="{Binding LatestVersion}" />
                            <!--  インストールボタン（未インストール）  -->
                            <ui:Button
                                Grid.Row="0"
                                Grid.Column="2"
                                Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Content="{x:Static properties:Resources.Install}"
                                DockPanel.Dock="Right"
                                Icon="{ui:SymbolIcon ArrowDownload24}"
                                IsEnabled="{Binding CanInstall}"
                                Style="{StaticResource InstallButtonStyle}"
                                Visibility="{Binding IsNotInstalled, Converter={StaticResource b2vConv}}" />

                            <!--  更新ボタン（更新あり）  -->
                            <ui:Button
                                Grid.Row="0"
                                Grid.Column="2"
                                Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Content="{x:Static properties:Resources.Update}"
                                DockPanel.Dock="Right"
                                Icon="{ui:SymbolIcon ArrowSync24}"
                                IsEnabled="{Binding CanInstall}"
                                Style="{StaticResource InstallButtonStyle}"
                                Visibility="{Binding CanUpdate, Converter={StaticResource b2vConv}}" />

                            <Label
                                Grid.Row="1"
                                Grid.Column="0"
                                Content="{x:Static properties:Resources.InstalledVersionLabel}"
                                Visibility="{Binding IsInstalled, Converter={StaticResource b2vConv}}" />
                            <ui:TextBlock
                                Grid.Row="1"
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Text="{Binding InstalledVersion}"
                                Visibility="{Binding IsInstalled, Converter={StaticResource b2vConv}}" />

                            <!--  アンインストールボタン（インストール済み）  -->
                            <ui:Button
                                Grid.Row="1"
                                Grid.Column="2"
                                Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Content="{x:Static properties:Resources.Uninstall}"
                                DockPanel.Dock="Right"
                                Icon="{ui:SymbolIcon Delete24}"
                                IsEnabled="{Binding IsInstalling, Converter={x:Static data:InverseBoolConverter.Default}}"
                                Style="{StaticResource InstallButtonStyle}"
                                Visibility="{Binding IsInstalled, Converter={StaticResource b2vConv}}" />

                        </Grid>

                        <Separator Margin="0,4,0,4" />

                        <!--  リンク  -->
                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                            <ui:HyperlinkButton
                                Content="{x:Static properties:Resources.ProjectUrl}"
                                Icon="{ui:SymbolIcon Globe24}"
                                NavigateUri="{Binding ProjectUrl}"
                                Visibility="{Binding HasProjectUrl, Converter={StaticResource b2vConv}}" />

                            <ui:HyperlinkButton
                                Content="{x:Static properties:Resources.LicenseUrl}"
                                Icon="{ui:SymbolIcon Document24}"
                                NavigateUri="{Binding LicenseUrl}"
                                Visibility="{Binding HasLicenseUrl, Converter={StaticResource b2vConv}}" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <ProgressBar
                    Grid.Row="1"
                    Height="4"
                    Margin="0,4"
                    IsIndeterminate="True"
                    Visibility="{Binding IsReadmeLoading, Converter={StaticResource b2vConv}}" />

                <md:MarkdownScrollViewer
                    Grid.Row="2"
                    Margin="0,8,0,0"
                    ClickAction="OpenBrowser"
                    Markdown="{Binding ReadmeMarkdown}"
                    MarkdownStyle="{StaticResource mdStyle}"
                    Visibility="{Binding HasReadme, Converter={StaticResource b2vConv}}">
                    <md:MarkdownScrollViewer.Plugins>
                        <mdp:MdXamlPlugins>
                            <ctrl:CustomPluginSetup />
                        </mdp:MdXamlPlugins>
                    </md:MarkdownScrollViewer.Plugins>
                </md:MarkdownScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
